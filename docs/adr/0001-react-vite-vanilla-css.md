# Use React, Vite, and Vanilla CSS

Status: superseded by ADR-0002

We decided to build the portfolio using React (bootstrapped with Vite) and style it using Vanilla CSS. This provides a component-based structure for dynamic interactive elements (like the AI Playground) while ensuring maximum flexibility, performance, and customization in styling without the overhead of external CSS frameworks.
